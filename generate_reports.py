def generate_reports():
    #this should be a single function that generates the report files by referencing the databases
    #using Travis's database class
    print("this function is incomplete, but it should generate the reports")